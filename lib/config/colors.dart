import 'package:flutter/material.dart';

class AppColors {
  // 🌞 Light Theme Colors
  static const lightBackground = Color(0xFFFDFDFD);
  static const lightPrimary = Color(0xFF0066FF); // Vivid Blue
  static const lightSecondary = Color(0xFF00C896); // Aqua Green
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightError = Color(0xFFEF5350); // Softer Material red
  static const lightTextPrimary = Color(0xFF1A1A1A);
  static const lightTextSecondary = Color(0xFF666666); // Slightly lighter for readability
  static const lightCard = Color(0xFFF0F2F5); // Soft grey card background
  static const lightDivider = Color(0xFFE0E0E0); // Subtle light divider

  // 🌚 Dark Theme Colors
  static const darkBackground = Color(0xFF121212); // Material Design dark base
  static const darkPrimary = Color(0xFF339DFF); // Softer Blue
  static const darkSecondary = Color(0xFF00D7A7); // Bright Teal
  static const darkSurface = Color(0xFF1C1C1E);
  static const darkError = Color(0xFFEF5350); // Matched with light error
  static const darkTextPrimary = Color(0xFFF5F5F5);
  static const darkTextSecondary = Color(0xFFB0B0B0);
  static const darkCard = Color(0xFF1E1F23); // Card background
  static const darkDivider = Color(0xFF2A2A2A);
}
