// import 'package:flutter/material.dart';
//
// class AppColors {
//   // 🌞 Light Theme Colors
//   static const lightBackground = Color(0xFFFDFDFD);
//   static const lightPrimary = Color(0xFF0066FF); // Vivid Blue
//   static const lightSecondary = Color(0xFF00C896); // Aqua Green
//   static const lightSurface = Color(0xFFFFFFFF);
//   static const lightError = Color(0xFFEF5350); // Softer Material red
//   static const lightTextPrimary = Color(0xFF1A1A1A);
//   static const lightTextSecondary = Color(0xFF666666); // Slightly lighter for readability
//   static const lightCard = Color(0xFFF0F2F5); // Soft grey card background
//   static const lightDivider = Color(0xFFE0E0E0); // Subtle light divider
//
//   // 🌚 Dark Theme Colors
//   static const darkBackground = Color(0xFF121212); // Material Design dark base
//   static const darkPrimary = Color(0xFF339DFF); // Softer Blue
//   static const darkSecondary = Color(0xFF00D7A7); // Bright Teal
//   static const darkSurface = Color(0xFF1C1C1E);
//   static const darkError = Color(0xFFEF5350); // Matched with light error
//   static const darkTextPrimary = Color(0xFFF5F5F5);
//   static const darkTextSecondary = Color(0xFFB0B0B0);
//   static const darkCard = Color(0xFF1E1F23); // Card background
//   static const darkDivider = Color(0xFF2A2A2A);
// }
import 'package:flutter/material.dart';

class AppColors {
  // 🌞 Advanced Light Theme Colors
  static const lightBackground = Color(0xFFF9FBFD); // Soft bluish white
  static const lightPrimary = Color(0xFF1E40AF); // Deep Indigo (trustworthy, bold)
  static const lightSecondary = Color(0xFF10B981); // Emerald Green (fresh & vibrant)
  static const lightSurface = Color(0xFFDFDDDD);
  static const lightError = Color(0xFFDC2626); // Rich error red
  static const lightTextPrimary = Color(0xFF111827); // Near-black
  static const lightTextSecondary = Color(0xFF6B7280); // Cool grey
  static const lightCard = Color(0xFFF1F5F9); // Soft blue-grey surface
  static const lightDivider = Color(0xFFE2E8F0); // Light divider, barely-there

  // 🌚 Advanced Dark Theme Colors
  static const darkBackground = Color(0xFF0F172A); // Navy-toned dark (elegant)
  static const darkPrimary = Color(0xFF3B82F6); // Vivid Blue (for highlights)
  static const darkSecondary = Color(0xFF22D3EE); // Bright Cyan (modern glow)
  static const darkSurface = Color(0xFF1E293B); // Dark surface, not too flat
  static const darkError = Color(0xFFEF4444); // Sharp red for contrast
  static const darkTextPrimary = Color(0xFFF8FAFC); // Clean near-white
  static const darkTextSecondary = Color(0xFF94A3B8); // Cool soft grey
  static const darkCard = Color(0xFF1E293B); // Subtle elevation
  static const darkDivider = Color(0xFF334155); // Low-contrast divider
}
